# i use a systemd timer for this
# to avoid sending too many requests
auto-reload no
reload-threads 4
browser zen-browser

cleanup-on-quit yes
text-width 80
# vim-ish keys
bind-key h quit
bind-key j down
bind-key k up
bind-key l open
bind-key H prev-feed
bind-key L next-feed
bind-key m toggle-article-read


include ~/.config/newsboat/theme
